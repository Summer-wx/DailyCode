#include<stdio.h>
/*p条直线(交于同一点)可以把平面划分为2*p个区间，而剩下的n-p条直线，
对于第i(i>p)条直线，要最多的划分平面，就要和前i-1条直线全部相交，
且不出现3条以上直线共点的情况，所以，第i条直线可以在平面上多划分出i个区间
f(i)=2*p(i<=p)=f(i-1)+i (i>p)
*/
int main(){
	int n,p,sum=0,i;
	scanf("%d %d",&n,&p);
	sum+=2*p;
	for(i=p+1;i<=n;i++){
		sum+=i;
	}
	printf("%d",sum);
	return 0;
}
