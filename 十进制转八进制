#include<stdio.h>
#include<math.h>
void bjz(int n){
	int i,j;
	for(i=n;i>=1;i--){
		for(j=1;j<n;j++){
			if(pow(8,i)<=n && j*pow(8,i)<=n){
				printf("%d",j);
				n=n-j*pow(8,i);
				break;
			}
		}
	}
	printf("%d",n);	
}
int main() {
    int n;
    scanf("%d",&n);
    bjz(n);
    return 0;
}
